"""The fargate package: host-side runner for a job that runs in an ECS task.

Imported as the openfactory-aws add-on's in-tree equivalent — registered through the
`box_runner.fargate` entry point (see pyproject) so `remote_box(\"fargate\")` resolves it."""
